server:
    port: 8888
    #config-client 默认找8888这个端口 需要查询如何覆盖

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/huihaoweishui/cloud-config-demo
          search-paths: repo
          username: huihaoweishui@163.com
          password: xuechen106712
      label: master
